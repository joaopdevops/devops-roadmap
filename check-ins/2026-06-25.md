# Daily Log — 2026-06-25 (Day 32)

**Phase:** Month 01 — Networking Fundamentals (CCNA)
**Mode:** Conceptual session (foundation rebuild)

## 🇬🇧 English

**Decision of the day**
- Chose to **restart Lab 2.4 from scratch** — the MTU concept wasn't solid, and moving forward on a weak foundation isn't worth it. No lab advanced today; the day was about rebuilding the concept.

**Subnet drill**
- `/27` → first pass wrong (calculated it as a `/26`: 62 usable, mask `.192`). Root cause: used 6 host bits instead of 5. Second pass nailed it: 30 usable (`2^5-2`), mask `255.255.255.224` with the `256-32=224` math written out.
- Lesson: **anchor on host bits first** (`32 - CIDR`), everything else hangs off that. Built the `/24→/30` ruler (8,7,6,5,4,3,2 host bits).

**Concept rebuilt — MTU**
- What MTU is and why a size limit exists (mail-truck / box analogy).
- What a network interface actually is. Ran `ip link show` on the real machine and read the output: active interface = `wlp0s20f3` (wifi), MTU 1500; recognized `lo` (virtual loopback, MTU 65536) and `docker0` (software virtual — the living example of the MTU problem in K8s/VPN).

**Next**
- The two outcomes when a packet is larger than the MTU: fragment **or** reject (DF bit). Then see fragmentation live with `tcpdump`.

---

## 🇧🇷 Português

**Decisão do dia**
- Optei por **refazer o Lab 2.4 do zero** — o conceito de MTU não estava sólido, e não vale avançar mal. Nenhum lab avançou hoje; o dia foi de reconstruir o conceito.

**Drill de máscara**
- `/27` → 1ª rodada errada (calculou como `/26`: 62 úteis, máscara `.192`). Raiz: usou 6 bits de host em vez de 5. 2ª rodada cravada: 30 úteis (`2^5-2`), máscara `255.255.255.224` com a conta `256-32=224` escrita.
- Lição: **ancorar nos bits de host primeiro** (`32 - CIDR`), todo o resto pendura nisso. Montei a régua `/24→/30` (8,7,6,5,4,3,2 bits de host).

**Conceito reconstruído — MTU**
- O que é MTU e por que existe limite de tamanho (analogia caminhão / caixa dos Correios).
- O que é, de fato, uma interface de rede. Rodei `ip link show` na máquina real e li a saída: interface ativa = `wlp0s20f3` (wifi), MTU 1500; reconheci `lo` (loopback virtual, MTU 65536) e `docker0` (virtual de software — o exemplo vivo do problema de MTU em K8s/VPN).

**Próximo**
- As duas saídas quando um pacote é maior que a MTU: fragmentar **ou** rejeitar (DF bit). Depois, ver a fragmentação ao vivo com `tcpdump`.
