# Daily Log — 2026-05-11 (Day 15)

**Phase / Fase:** Month 01 — Networking Fundamentals (CCNA)
**Topic / Tópico:** Lab 2.2 Stage 3 — IOS CLI + configuration / Etapa 3 — IOS CLI + configuração

**🇬🇧 English**
- IOS CLI mini-lesson: the 4 modes (User EXEC, Privileged EXEC, Global Config, Interface Config).
- Configured 4 router interfaces and 8 PCs; per interface `enable → configure terminal → interface → ip address → no shutdown`, saved with `write memory`, verified with `show ip interface brief`.

**🇧🇷 Português**
- Mini-aula de IOS CLI: os 4 modos (User EXEC, Privileged EXEC, Global Config, Interface Config).
- Configurei 4 interfaces do roteador e 8 PCs; por interface `enable → configure terminal → interface → ip address → no shutdown`, salvo com `write memory`, verificado com `show ip interface brief`.
