# Daily Log — 2026-07-02 (Day 38)

**Phase:** Month 01 — Networking Fundamentals (CCNA)
**Mode:** Minimum viable session (~15 min, busy day)

## 🇬🇧 English

**Mask drill — /28**
- `256 − 16 = 240` → `255.255.255.240`, block of 16, **14 usable hosts**. This exact mask was a real miss back in May (answered 248/`/29` by adjacent-drift); today it came out clean and instant. **Old weakness healed.** Drill streak: /26, /29, /30, /28.

**New study protocol debut — retrieval before re-reading**
- New routine: before opening any note, write down from memory what you remember about the day's topic; only then check the note to hunt for gaps. Re-reading feels like mastery — retrieval proves it.
- First run (topic: route decision) did its job immediately: **1 hit, 1 real misconception found, 1 gap**.
  - ✅ Correctly recalled the real `ip route` output (default via gateway, connected LAN, docker0).
  - ❌ Misconception caught: thought `via` meant "router doesn't have that route saved". Corrected: a line **with `via` IS a saved route** — it means "hand this to the **gateway** (middleman)"; **no `via`** = connected route, deliver directly. No matching route at all (not even default) → drop + ICMP Destination Unreachable.
  - 🕳️ Gap: forgot the golden rule — **longest prefix match** (most specific matching line wins; `/0` default is the last resort).
- Passive re-reading would have hidden that misconception. Retrieval exposed it in 2 minutes.

**Next**
- Re-test both weak points, then build the Lab 2.5 static-routing topology in Packet Tracer: ping fails without a route → add static route → ping succeeds.

---

## 🇧🇷 Português

**Drill de máscara — /28**
- `256 − 16 = 240` → `255.255.255.240`, bloco de 16, **14 hosts úteis**. Essa exata máscara foi um erro real em maio (respondi 248/`/29` por desvio adjacente); hoje saiu limpa e na hora. **Fraqueza antiga cicatrizada.** Sequência de drills: /26, /29, /30, /28.

**Estreia de protocolo de estudo — lembrar antes de reler**
- Nova rotina: antes de abrir qualquer nota, escrever de memória o que lembro do tema do dia; só depois conferir a nota caçando os buracos. Reler dá sensação de domínio — lembrar prova.
- Primeira rodada (tema: decisão de rota) já cumpriu o papel: **1 acerto, 1 erro conceitual real encontrado, 1 buraco**.
  - ✅ Lembrei certo a saída real do `ip route` (default via gateway, LAN conectada, docker0).
  - ❌ Erro pego: achava que `via` significava "roteador não tem aquela rota salva". Correção: linha **com `via` É rota salva** — significa "entregue ao **gateway** (intermediário)"; **sem `via`** = rota conectada, entrega direta. Sem rota nenhuma que case (nem default) → descarta + ICMP Destination Unreachable.
  - 🕳️ Buraco: esqueci a regra de ouro — **longest prefix match** (a linha mais específica que casa ganha; a default `/0` é o último recurso).
- Releitura passiva teria escondido esse erro. O recall expôs em 2 minutos.

**Próximo**
- Recobrar os 2 pontos fracos e montar a topologia de roteamento estático do Lab 2.5 no Packet Tracer: ping falha sem rota → adiciona rota estática → ping chega.
