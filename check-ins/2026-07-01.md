# Daily Log — 2026-07-01 (Day 37)

**Phase:** Month 01 — Networking Fundamentals (CCNA)
**Mode:** Short session (retest re-validation → **Lab 2.4 CLOSED**)

## 🇬🇧 English

**Mask drill — /30**
- `255.255.255.252` via the explicit rule `256 − 4 = 252`, block of 4, **2 usable hosts**. Tied straight into the real lab: the R1↔R2 road in Lab 2.5 is a /30 precisely because it only needs 2 IPs. Drill streak: /26, /29, /30.

**72h retest re-validated — 2/2**
1. **Bytes vs bits (application):** 300 Mbps plan, browser shows 37.5 MB/s — fraud? → `300 ÷ 8 = 37.5`. The ISP is delivering exactly what was sold. ✅
2. **Silent loss (application):** 2000-byte ping WITHOUT the DF bit through a path with an MTU-1500 link → the router fragments it, no ICMP comes back. ✅
   - **Refinement:** on screen the ping replies **normally** (router fragments, destination reassembles) — the *silence* is about the ICMP error, not the reply. That's what makes it dangerous: if a fragment is lost, nothing warns you.

**🏁 Lab 2.4 (ICMP/Fragmentation) — CLOSED**
- Full criteria met: Stage 1 (MTU/DF/PMTUD live on Linux) + Stage 2 (real fragmentation in tcpdump) + 24h retest + 72h surprise retest (2/2).
- **Month 01 now has a single lab left: 2.5 — Basic Routing.**

**Next**
- Build the Lab 2.5 topology in Packet Tracer (addressing table ready — see Day 36): ping fails without a route → add static route → ping succeeds.

---

## 🇧🇷 Português

**Drill de máscara — /30**
- `255.255.255.252` pela regra explícita `256 − 4 = 252`, bloco de 4, **2 hosts úteis**. Amarrado direto no lab real: a estrada R1↔R2 do Lab 2.5 é /30 exatamente porque só precisa de 2 IPs. Sequência de drills: /26, /29, /30.

**Reteste 72h revalidado — 2/2**
1. **Bytes vs bits (aplicação):** plano de 300 Mbps, navegador mostra 37,5 MB/s — roubo? → `300 ÷ 8 = 37,5`. A operadora entrega exatamente o que vendeu. ✅
2. **Perda silenciosa (aplicação):** ping de 2000 bytes SEM o DF bit por um caminho com link de MTU 1500 → o roteador fragmenta, nenhum ICMP volta. ✅
   - **Refino:** na tela o ping responde **normal** (roteador fragmenta, destino remonta) — o *silêncio* é do erro ICMP, não da resposta. É isso que a torna perigosa: se um fragmento se perde, nada te avisa.

**🏁 Lab 2.4 (ICMP/Fragmentação) — FECHADO**
- Critério completo: Etapa 1 (MTU/DF/PMTUD ao vivo no Linux) + Etapa 2 (fragmentação real no tcpdump) + reteste 24h + reteste 72h surpresa (2/2).
- **Mês 01 agora tem um único lab restante: 2.5 — Roteamento Básico.**

**Próximo**
- Montar a topologia do Lab 2.5 no Packet Tracer (tabela de endereçamento pronta — ver Dia 36): ping falha sem rota → adiciona rota estática → ping chega.
